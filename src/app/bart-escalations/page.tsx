import BARTPreview from '@/components/bart/BARTPreview';

export default function BARTEscalationsPage() {
  return (
    <BARTPreview
      eyebrow="BART preview"
      title="Escalation queue"
      description="Review active escalations, scope mismatches, and recommended actions awaiting human approval."
    />
  );
}
