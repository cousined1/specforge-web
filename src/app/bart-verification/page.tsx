import BARTPreview from '@/components/bart/BARTPreview';

export default function BARTVerificationPage() {
  return (
    <BARTPreview
      eyebrow="BART preview"
      title="Verification checkpoints"
      description="Audit verification gates, scope checks, and provenance signals for each orchestration run."
    />
  );
}
