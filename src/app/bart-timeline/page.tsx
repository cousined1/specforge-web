import BARTPreview from '@/components/bart/BARTPreview';

export default function BARTTimelinePage() {
  return (
    <BARTPreview
      eyebrow="BART preview"
      title="Execution timeline"
      description="Trace orchestration events, plan mutations, and verification checkpoints across each run."
    />
  );
}
