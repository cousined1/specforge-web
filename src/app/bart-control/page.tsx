import BARTPreview from '@/components/bart/BARTPreview';

export default function BARTControlPanel() {
  return (
    <BARTPreview
      eyebrow="BART preview"
      title="Execution control panel"
      description="Monitor orchestration runs, confidence levels, blocked tickets, and escalation gates in one dashboard."
    />
  );
}
