import BARTPreview from '@/components/bart/BARTPreview';

export default function BARTTaskGraphPage() {
  return (
    <BARTPreview
      eyebrow="BART preview"
      title="Task graph"
      description="Visualize task dependencies, blocked tickets, and execution order across orchestration runs."
    />
  );
}
